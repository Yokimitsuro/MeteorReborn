<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\MeteorReborn.Common\MeteorReborn.Common.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- PM Lobby Database usa SQL crudo via MySqlConnection. LANG-ADAPT: Npgsql raw (1:1
         con MySql, solo cambia driver). EF Core ORM se reserva para servidores que lo necesiten. -->
    <PackageReference Include="Npgsql" Version="9.0.2" />
    <PackageReference Include="MoonSharp" Version="2.0.0" />
    <PackageReference Include="Serilog" Version="4.2.0" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.0.0" />
  </ItemGroup>

</Project>
