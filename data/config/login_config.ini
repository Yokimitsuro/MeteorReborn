; FINISH-PM (MR-original infrastructure) — Login server config para Docker.
; Login Server es MR-original (PM-MISSING). Lee mismos credenciales DB que Lobby.

[Database]
host=postgres
port=5432
database=meteor
username=meteor
password=meteor
