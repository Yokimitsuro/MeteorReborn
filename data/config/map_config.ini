; PM port: Data/map_config.ini (PM-COMPLETE → verbatim para [General]; [Database] LANG-ADAPT MySQL→Postgres).
; PM defaults referenciados: Map Server/ConfigConstants.cs:55-63.

[General]
; Usa el hostname del servicio docker; resuelve a la IP del propio container vía DNS y matchea
; server_zones.serverIp = 'map' que LoadZoneList usa como filtro.
server_ip=map
server_port=1989
showtimestamp=true

[Database]
worldid=1
host=postgres
port=5432
database=meteor
username=meteor
password=meteor
